Given a buggy program, users as tasked to find the bug with/without stlviz.

Users are allowed to modify the code (such as adding std::cout or changing the input), but only need to identify the bug to finish.

Algorithm is guaranteed to be wrong for the given input.

There is only a single bug preventing the algorithm from working correctly.

Bounds are all small enough that integer overflow is not a concern.
