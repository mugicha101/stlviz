#include <SFML/Graphics.hpp>
#include <stlviz.hpp>
#include <thread>
#include <iostream>

using namespace std::chrono_literals;

// debug program

int main() {
    vstd::vector<int> vec;
    std::this_thread::sleep_for(100s);
}
